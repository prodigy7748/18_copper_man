<h2><%= t('users.sign_in') %></h2>

<%= form_for(@user, url: sign_in_user_path, method: 'post') do |form| %>
  <div class="form-group">
    <%= form.label t('activerecord.attributes.user.email'), class:"mb-2
    " %>
    <%= form.text_field :email, class:"form-control"%>
  </div>
  <br>
  <div class="form-group">
    <%= form.label t('activerecord.attributes.user.password_digest'), class:"mb-2
    " %>
    <%= form.password_field :password, class:"form-control"%>
    <br>
    <%= form.submit t('users.sign_in'), :class => "btn btn-primary"%>
  </div>
<% end %>
